#include <stdio.h> // <-- import input/output from the standard library

// create a main function
// the compiler looks for the main function
// in order to execute code

// declare a function called main
// it returns an integer
// it takes in no arguments
int main(void)
{
    // this prints Hello world! with a new line to stdout
    printf("Hello world!\n");

    // we return 0 because nothing went wrong
    // there are zero errors
    return 0;
}